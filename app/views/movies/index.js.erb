$('#content').append("<%= render(partial: 'movie', collection: @movies, format: 'html') %>");
<% if @current_page == @total_pages %>
	$('#view-more').remove();
<% else %>
	$('#view-more a').attr('href', '<%= link_to("Load more", movies, page: @current_page + 1) %>');
<% end %>
